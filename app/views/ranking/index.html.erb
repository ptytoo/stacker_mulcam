<header class="bg-dark text-white">
  <div class="container text-center">
    <div class="justify-content-center">
      <!-- Searching Section -->
       <div class="subscribe section bg-dark py-1">
         <h4 class="section-title text-center text-white m-5">순위 페이지</h4>
         <!-- <p class="text-muted col-md-6 text-center mx-auto"></p> -->
       </div>
    </div>
  </div>
</header>
<br>

<div class="container">
  <div class="row">
    <div class="col card">
      <strong>나와 스택이 비슷한 유저</strong><br>
      <% if user_signed_in? && current_user.indi? %>
          <% @users.each do |u| %>
            <%us = User.find_by(id: u.id)%>
            <i class="fa fa-user-circle-o fa-2x" aria-hidden="true"> <%= us.nickname %></i>  (<%= u.cnt %>)
            <br>
          <% end %>
        <br>
      <% else %>
        <div class="alert alert-info alert-dismissible fade show" role="alert">
          개인 계정 로그인 시, <br>나와 비슷한 스택을 가진 유저를 추천해 드립니다.
        </div>
      <% end %>
    </div>
    <div class="col card">
      <strong>개인 회원들의 순위</strong><br>
      <% @indi_users.each do |t|%>
        <i class="fa fa-user-circle-o fa-2x" aria-hidden="true"> <%= t.nickname %></i>  (<%= t.stack_cnt%>)
        <br><br>
      <% end %>
    </div>
  </div>
</div>
<br><br>
<style>
  body {
    background: #f9f9f9;
    text-align: center;
  }

  a #logo_img{
    -webkit-filter: grayscale(100%);
    filter: gray;
    padding: 10px;
  }
  a:hover #logo_img{
    -webkit-filter: grayscale(0%);
    filter: none;
  }
  .card {
    background: #fff;
    border-radius: 2px;
    display: inline-block;
    height: 600px;
    margin: 1rem;
    position: relative;
    width: 300px;
  }

  .card_r {
    border-radius: 2px;
    margin: 2px;
  }
  .card-3 {
    box-shadow: 0 10px 20px rgba(0,0,0,0.19), 0 6px 6px rgba(0,0,0,0.23);
  }
</style>
