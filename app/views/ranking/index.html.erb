<h1>나와 비슷한 스택을 가진 유저</h1>
<% @users.each do |u| %>
  <%= u.user.nickname %> | 
  <%= u.cnt %>개<br>
<% end %>
<br>

<h1>개인 회원 순위 (스택 갯수 기준)</h1>
<% @indi_users.each do |t|%>
  <%= t.nickname%>
  <%= t.stack_cnt%>
  <br>
<% end %>

<h1>기업 회원 순위 (스택 갯수 기준 / 상위 10개)</h1>
<% @services_cnt.each do |t|%>
  <%cpy = Service.find_by(name: t.name)%>
  <img src="<%=cpy.logo_url%>" height = "50">
  <%= t.stack_cnt%>
  <br>
<% end %>
