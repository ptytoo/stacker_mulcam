<p id="notice"><%= notice %></p>


<p>
  <strong>Name:</strong>
  <%= @company_field.name %>
</p>

<table>
  <thead>
    <tr>
      <th>Compnany Name</th>
      <th>Site_url</th>
      <th>Logo_url</th>
      <th>Describe</th>
      <th colspan="3"></th>
    </tr>
</thead>

  <tbody>
    <% @companies.each do |company| %>
      <tr>
        <td><%= company.name %><%= link_to '<button type="button">상세보기</button>'.html_safe, services_path %></td>
        <td><%= company.site_url %></td>
        <td><%= company.logo_url %></td>
        <td><%= company.describe %></td>
      </tr>
    <% end %>
  </tbody>
</table>



<%= link_to 'Edit', edit_company_field_path(@company_field) %> |
<%= link_to 'Back', company_fields_path %>
