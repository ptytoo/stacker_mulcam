<header class="bg-dark text-white">
  <div class="container text-center">
    <div class="justify-content-center">
      <!-- Searching Section -->
       <div class="subscribe section bg-dark py-1">
         <h4 class="section-title text-center text-white m-5">원하는 분야의 스택 사용 확인</h4>
         <p class="text-muted col-md-6 text-center mx-auto">간단한 설명 들어갈 부분</p>
       </div>
    </div>
  </div>
</header>
<div id="b_form">
  <% c_field_list = ["Social media/Communication","Edu Tech","Utility","Fintech","Game",
  "E-Commerce","Foodtech","Contents","Healthcare","Ad/Marketing","IoT","Analytics","Sharing Economy"] %>
  <% @company_fields.each_with_index do |field, index| %>
  <div>
      <p><%= field.name%></p>
      <% @companies.each do |c| %>
        <% if c.company_field_id == (index+1) %>
          <%= link_to "<button type='button' class='btn btn-outline-primary btn-pill'>#{c.name}</button>".html_safe, detail_company_path(c.id), method: :get %>
          <%#= c.name%>
        <% end %>
      <% end %>
    <% end %>
  </div>
</div>

<style>
  #b_form {
    text-align: center;
  }
</style>
