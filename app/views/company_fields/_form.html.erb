<%= form_for(@company_field) do |f| %>
  <% if @company_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company_field.errors.count, "error") %> prohibited this company_field from being saved:</h2>

      <ul>
      <% @company_field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
